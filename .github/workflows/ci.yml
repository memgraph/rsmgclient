name: CI

on: [push]

jobs:
  build_and_test:
    strategy:
     matrix:
        platform: [ubuntu-18.04]
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Run Tests
        run: |
          cargo build --verbose
          cargo run --verbose
          cargo test --verbose
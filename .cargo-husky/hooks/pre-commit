#!/bin/sh

set -eu


if cargo test --all; then \
    echo '+cargo test --all'; \
fi
if cargo clippy; then \
    echo '+cargo clippy'; \
fi
if cargo clippy --fix -Z unstable-options --allow-dirty --allow-staged; then \
    echo '+clippy --fix -Z unstable-options --allow-dirty --allow-staged'; \
fi
